language: node_js

node_js:
  - "9.11.1"

sudo: true

env:
  - CXX=g++-4.8
  - NODE_ENV=testing
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.8

services:
  - postgresql

before_script:
  - psql -c "CREATE DATABASE nodetesting;" -U postgres
  - node_modules/.bin/sequelize db:migrate
